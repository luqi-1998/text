import HTTP from "../utils/fetch";

export const postUploadFile = () => {
  return HTTP.get("./api/ss");
};
export const mergeUploadFile = () => {
  return HTTP.get("./api/ss");
};
export const verifyUpload = () => {
  return HTTP.get("./api/ss");
};
